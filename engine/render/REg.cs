using System;
using System.Collections.Generic;
using Microsoft.Xna.Framework.Graphics;

class reg {
    Dictionary<String, Texture2D> textures;

    void request(String name) {
        // search for name in dict

        if not
            // new entry

        // return ref to texture
    }

    void finalize() {
        // load all textures
    }
}